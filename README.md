Clone this repository.
Run npm install to install all dependencies.
Create catedb postgres database then run the BackEnd/createDB/dbTables.sql script to create databse table.
Update the .env file with your values.
Start the server by running node index.js.The linke will appear with the below format to run the application.
      http://localhost:Portnumber
Run the application by paste the link in the browser.
Create a new user the you can login and using the application. 
